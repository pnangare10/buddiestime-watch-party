{
  "name": "fluffles-watch-party",
  "version": "1.0.0",
  "description": "Watch party sync server",
  "scripts": {
    "start": "node server/server.js",
    "postinstall": "cd server && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
