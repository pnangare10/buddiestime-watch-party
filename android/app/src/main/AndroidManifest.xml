<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.buddiestime.watchparty">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
        Self-update over the network (see UpdateManager). This never installs silently:
        the user must additionally allow this app as an install source in system settings,
        and Android shows its own confirmation dialog for every install. The grant is also
        revocable — Android 11+ clears it under app hibernation — so it is re-checked
        before every attempt rather than once at setup.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Fluffles">

        <activity
            android:name=".RoomsHomeActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WelcomeSetupActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="false" />

        <activity
            android:name=".PairingRedeemActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="true">
            <!--
                autoVerify is required, not optional: on Android 12+ an unverified
                https filter is dropped from link handling altogether, so invites
                open in a browser and the app is never offered. Verification checks
                https://<pairing_host>/.well-known/assetlinks.json — see the matching
                route in server/server.js; the two must ship together.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="@string/pairing_host" android:pathPrefix="/pair/" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="false" />

        <activity
            android:name=".ServiceSelectorActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Receives PackageInstaller session results, including the STATUS_PENDING_USER_ACTION
             intent that shows the system's install confirmation. Not exported: only the
             platform delivers to it, via the PendingIntent we hand to session.commit(). -->
        <receiver android:name=".UpdateInstallReceiver" android:exported="false" />

        <service android:name=".FcmService" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
