<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Solid, not the old 40%-alpha chat_scrim: incoming messages sat over bright
         video and were unreadable. Matches the input row's fill. -->
    <solid android:color="@color/plum_surface" />
    <stroke android:width="1dp" android:color="@color/chat_bubble_stroke" />
    <corners android:radius="16dp" />
</shape>
