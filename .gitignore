node_modules/
*.log
.DS_Store
.worktrees/

# Secrets — never commit
.env
.env.local
server/.env
server/.env.local

# Android build outputs
android/.gradle/
android/app/build/
android/build/
android/local.properties

# Release keystore — never commit this
*.keystore
release.keystore
android/app/release.keystore

# emulator role pinning is machine-specific (device serials)
tools/emu.config.json
*.jks
